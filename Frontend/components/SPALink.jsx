'use client';import { useNavigation } from '@/context/NavigationContext';import { ReactNode, MouseEvent } from 'react';export function SPALink({    href,    children,    className = '',    ...props}) {    const { navigateTo } = useNavigation();    const handleClick = (e) => {        if (href.startsWith('/')) {            e.preventDefault();            let view = href;            if (view.startsWith('/dashboard/')) {                view = view.replace('/dashboard/', 'dashboard/');            } else if (view.startsWith('/')) {                view = view.substring(1);            }            navigateTo(view);        }    };    return (        <a            href={href}            onClick={handleClick}            className={className}            {...props}        >            {children}        </a>    );}