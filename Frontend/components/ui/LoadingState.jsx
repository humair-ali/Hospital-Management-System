import React from 'react';export const LoadingState = ({    message = 'Synchronizing Systems...',    variant = 'primary'}) => {    const colors = {        primary: 'text-primary-600 border-primary-100 border-t-primary-600',        emerald: 'text-emerald-600 border-emerald-100 border-t-emerald-600',        blue: 'text-blue-600 border-blue-100 border-t-blue-600',        indigo: 'text-indigo-600 border-indigo-100 border-t-indigo-600',    };    const colorClass = colors[variant] || colors.primary;    const textBaseColor = colorClass.split(' ')[0];    return (        <div className="flex flex-col items-center justify-center min-h-[60vh] gap-10">            <div className={`animate-spin h-20 w-20 border-[6px] rounded-full border-gray-100 ${colorClass.split(' ').slice(2).join(' ')}`}></div>            <div className="flex flex-col items-center gap-4">                <p className="text-sm font-black text-gray-800 uppercase tracking-[0.3em]">                    {message}                </p>                <div className="w-64 h-1.5 bg-gray-100 rounded-full overflow-hidden relative shadow-inner">                    <div className={`absolute inset-0 bg-current opacity-10 ${textBaseColor}`}></div>                    <div className={`absolute inset-0 bg-current w-1/3 animate-loading-progress ${textBaseColor}`}></div>                </div>            </div>        </div>    );};