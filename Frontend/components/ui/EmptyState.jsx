import React from 'react';export const EmptyState = ({    icon: Icon,    title,    subtitle,    actions}) => {    return (        <div className="py-24 flex flex-col items-center justify-center text-center">            {Icon && (                <div className="w-24 h-24 bg-primary-50 rounded-[2rem] flex items-center justify-center text-primary-200 mb-8 shadow-inner border border-primary-100">                    <Icon size={40} className="text-primary-300" />                </div>            )}            <h3 className="text-3xl font-black text-gray-900 uppercase tracking-tighter mb-4">                {title}            </h3>            <p className="text-sm font-bold text-gray-600 uppercase tracking-widest max-w-sm mx-auto mb-10 leading-relaxed">                {subtitle}            </p>            {actions && (                <div className="flex gap-4">                    {actions}                </div>            )}        </div>    );};