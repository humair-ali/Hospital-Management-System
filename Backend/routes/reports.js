const express = require('express');const { getDailyReport, getMonthlyReport, getAnnualReport } = require('../controllers/reportsController');const { verifyToken } = require('../middleware/auth');const { authorize } = require('../middleware/rbac');const router = express.Router();router.use(verifyToken);const reportsRoles = ['admin', 'accountant'];router.get('/daily', authorize(reportsRoles), getDailyReport);router.get('/monthly', authorize(reportsRoles), getMonthlyReport);router.get('/annual', authorize(reportsRoles), getAnnualReport);module.exports = router;